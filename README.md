# Dashboard (Next.js + NestJS + MySQL)

## Structure
- front/ (Next.js)
- back/ (NestJS)
- MySQL schema: dashboardv2

## Setup
### Backend
cd back
cp .env.example .env
npm install
npm run start:dev

### Frontend
cd front
cp .env.example .env.local
npm install
npm run dev

